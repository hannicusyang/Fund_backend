# 后端数据源架构改造计划

## 一、当前架构分析

### 1.1 数据源使用现状

| 模块 | 主要数据源 | 备用数据源 | 缓存情况 |
|------|-----------|-----------|---------|
| 股票K线 (stock_kline.py) | akshare | baostock | 无 |
| 股票实时 (stock_realtime.py) | akshare | 无 | 简单内存缓存 |
| 股票回测 (stock_backtest.py) | tushare | baostock | 无 |
| 基金详情 (fund_detail.py) | akshare | tushare | 无 |
| 基金实验室 (fund_lab.py) | tushare | akshare | 数据库 |
| 基金估值 (fund_estimation_scheduler.py) | tushare | akshare | Redis + MySQL |
| 股票筛选 (stock_screening.py) | tushare | 无 | 无 |

### 1.2 现有缓存

- **Redis**: 仅用于基金估值计算的中间结果存储
- **内存缓存**: `_api_cache` 简单字典，有5分钟过期时间
- **MySQL**: 基金净值历史、股票因子数据等

### 1.3 问题总结

1. 数据源分散，没有统一管理
2. tushare 没有作为主要数据源
3. 缓存层不完善，高频请求直接打第三方API
4. 没有实现 "tushare -> 备用 -> Redis -> MySQL" 的层级架构

---

## 二、改造目标

### 2.1 架构目标

```
请求 → Tushare → 备用数据源 → Redis缓存 → MySQL → 返回
         ↓          ↓            ↓          ↓
      (优先)    (失败时)    (高频数据)  (历史数据)
```

### 2.2 缓存策略

| 数据类型 | 缓存策略 | 过期时间 |
|---------|---------|---------|
| 实时行情 (股票/基金) | Redis | 5-15秒 |
| 日K线数据 | Redis | 5分钟 |
| 基金净值 | Redis + MySQL | 实时 + 历史 |
| 股票列表/基本信息 | MySQL | T+1更新 |
| 因子数据 | MySQL | 定时更新 |

---

## 三、改造计划

### 3.1 第一阶段：统一数据源层

#### 3.1.1 创建统一数据源管理器

**新建文件**: `utils/data_source_manager.py`

```python
class DataSourceManager:
    """
    统一数据源管理器
    优先级: tushare -> akshare -> baostock -> 本地缓存
    """
    
    # 数据源配置
    SOURCES = {
        'tushare': {...},      # 主数据源
        'akshare': {...},      # 备用1
        'baostock': {...},     # 备用2
    }
    
    # 缓存配置
    CACHE_CONFIG = {
        'realtime': {'ttl': 15, 'source': 'redis'},
        'kline': {'ttl': 300, 'source': 'redis'},
        'nav': {'ttl': 60, 'source': 'redis'},
        'basic': {'ttl': 86400, 'source': 'mysql'},
    }
    
    def get_stock_realtime(self, code):
        """获取股票实时数据"""
        # 1. 先查Redis缓存
        # 2. 缓存未命中，调用tushare
        # 3. tushare失败，调用akshare
        # 4. 写入Redis缓存
        # 5. 返回数据
    
    def get_fund_nav(self, fund_code, date):
        """获取基金净值"""
        # 类似逻辑
```

#### 3.1.2 改造 utils/tushare_api.py

- 添加完整的错误处理和重试机制
- 添加数据源健康检查
- 添加请求频率控制

### 3.2 第二阶段：完善缓存层

#### 3.2.1 创建缓存服务

**新建文件**: `services/cache_service.py`

```python
class CacheService:
    """统一缓存服务"""
    
    def __init__(self):
        self.redis = get_redis_client()
    
    # 股票相关缓存
    def get_stock_realtime(self, code)
    def set_stock_realtime(self, code, data, ttl=15)
    def get_stock_kline(self, code, period)
    def set_stock_kline(self, code, period, data, ttl=300)
    
    # 基金相关缓存  
    def get_fund_nav(self, fund_code, date)
    def set_fund_nav(self, fund_code, date, data, ttl=60)
    def get_fund_realtime(self, fund_code)
    def set_fund_realtime(self, fund_code, data, ttl=15)
    
    # 批量操作
    def batch_get_stock_realtime(self, codes)
    def batch_get_fund_realtime(self, codes)
```

#### 3.2.2 创建数据同步服务

**新建文件**: `services/data_sync_service.py`

```python
class DataSyncService:
    """数据同步服务 - 定时任务调用"""
    
    def sync_stock_realtime_to_redis(self):
        """同步股票实时数据到Redis"""
    
    def sync_fund_realtime_to_redis(self):
        """同步基金实时数据到Redis"""
    
    def sync_stock_kline_to_cache(self):
        """同步K线数据到缓存"""
    
    def sync_fund_nav_to_mysql(self):
        """同步基金净值到MySQL"""
```

### 3.3 第三阶段：改造现有接口

#### 3.3.1 股票接口改造

| 文件 | 改造内容 |
|------|---------|
| stock_kline.py | 使用新的DataSourceManager |
| stock_realtime.py | 添加Redis缓存，优先从缓存读取 |
| stock_screening.py | 添加Redis缓存 |
| stock_backtest.py | 已完成tushare优先 |

#### 3.3.2 基金接口改造

| 文件 | 改造内容 |
|------|---------|
| fund_detail.py | 使用统一数据源，添加缓存 |
| fund_lab.py | 使用统一数据源，添加缓存 |
| fund_rank.py | 使用统一数据源，添加缓存 |

### 3.4 第四阶段：优化定时任务

#### 3.4.1 定时任务优化

| 任务 | 优化内容 |
|------|---------|
| sync_stock_realtime.py | 批量同步到Redis，缩短间隔 |
| fund_estimation_scheduler.py | 优先从Redis读取实时数据 |
| sync_stock_tushare.py | 基础数据同步到MySQL |
| sync_fund_holdings.py | 持仓数据同步 |

#### 3.4.2 新增定时任务

- `sync_realtime_to_redis.py`: 每分钟同步实时数据到Redis
- `cleanup_expired_cache.py`: 清理过期缓存

---

## 四、实施步骤

### 步骤1: 创建统一数据源管理器 (1天)
- [ ] 创建 `utils/data_source_manager.py`
- [ ] 实现股票/基金数据获取方法
- [ ] 实现缓存逻辑

### 步骤2: 完善缓存服务 (1天)
- [ ] 创建 `services/cache_service.py`
- [ ] 实现Redis缓存方法
- [ ] 实现MySQL回源逻辑

### 步骤3: 改造股票接口 (2天)
- [ ] 改造 stock_realtime.py
- [ ] 改造 stock_kline.py
- [ ] 改造 stock_screening.py

### 步骤4: 改造基金接口 (2天)
- [ ] 改造 fund_detail.py
- [ ] 改造 fund_lab.py
- [ ] 改造 fund_rank.py

### 步骤5: 优化定时任务 (1天)
- [ ] 优化现有定时任务
- [ ] 新增缓存同步任务

### 步骤6: 测试与优化 (1天)
- [ ] 压力测试
- [ ] 性能优化
- [ ] 监控告警

---

## 五、技术要点

### 5.1 缓存Key命名规范

```
stock:realtime:{code}        # 股票实时数据
stock:kline:{code}:{period}  # 股票K线数据
fund:realtime:{code}         # 基金实时数据
fund:nav:{code}:{date}      # 基金净值数据
stock:basic:{code}          # 股票基本信息
fund:basic:{code}           # 基金基本信息
```

### 5.2 异常处理

```python
try:
    data = tushare_api()
except TushareError:
    try:
        data = akshare_api()
    except:
        # 回源到MySQL/返回错误
```

### 5.3 频率控制

- tushare: 3次/秒 (官方限制)
- akshare: 2次/秒
- baostock: 5次/秒

---

## 六、风险与应对

| 风险 | 应对措施 |
|------|---------|
| tushare API不稳定 | 自动切换到akshare/baostock |
| Redis连接失败 | 回源到直接调用API |
| 数据一致性 | 定时全量同步校验 |
| 请求频率超限 | 实现请求队列和重试机制 |

---

## 七、预期效果

1. **响应速度提升**: 高频数据请求从API调用变为Redis读取，延迟从500ms+降至5ms以内
2. **API稳定性提升**: 多数据源冗余，单点故障不影响服务
3. **成本降低**: 减少不必要的API调用次数
4. **可维护性提升**: 统一的数据源管理，便于监控和优化
